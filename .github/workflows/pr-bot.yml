name: PR Bot
on: [workflow_dispatch]
jobs:
  search-for-prs:
    runs-on: ubuntu-latest
    environment: development
    steps:
      - name: GH Test 🧪
        run: gh api '/search/issues?q=is:open%20is:pr%20org:legalforce%20review-requested:calbert1209&advance_search=true'
